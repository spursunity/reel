export type TfallingSymbol = {
    image: any,
    width: number,
    startY: number,
    endY: number,
    positionX: number,
    delay: number,
    velocity: number
} ;
export type TsoundData = {
    src: string,
    id: string
} ;
